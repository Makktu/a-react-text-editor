import "./ButtonOne.css";

const ButtonOne = () => {
    return (
        <div>
            <button>This is ButtonOne from its own folder speaking.</button>
        </div>
    );
};

export default ButtonOne;
